[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta

[project]
name = "graphs_LightningRadeken"
version = "0.1.0"
description = "A Python library for graph algorithms including Dijkstra's shortest path"
author = [
    {name = "Malachi Mooty", email = "radeken2015@gmail.com"}
] 
readme = "README.md"
requires-python = ">=3.8"
license = {text="MIT"}